{% extends 'base.html.twig' %}

{% block title %}Chocolaterie{% endblock %}

{% block body %}
    <h1>Chocolaterie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Ville</th>
                <td>{{ chocolate_shop.city }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chocolate_shop_index') }}">Retour à la liste</a>

    <a href="{{ path('app_chocolate_shop_edit', {'id': chocolate_shop.id}) }}">Modifier</a>

    {{ include('chocolate_shop/_delete_form.html.twig') }}
{% endblock %}
